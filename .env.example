# Public Vite env; this is embedded into the built frontend.
# Set this in Vercel Project Settings -> Environment Variables.
VITE_DEFAULT_API_URL=https://your-api.example.com/v1

# Only set true if your deployment actually provides /api-proxy.
VITE_API_PROXY_AVAILABLE=false
